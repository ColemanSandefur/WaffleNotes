---
import SeriesPartBadge, {
  type SeriesPartBadgeProps,
} from "./preact/SeriesPartBadge";

interface Props extends SeriesPartBadgeProps {}

const props = Astro.props;
---

<SeriesPartBadge {...props} />
