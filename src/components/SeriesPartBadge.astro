---
import SeriesPartBadge from "./preact/SeriesPartBadge";

interface Props {
  numPosts: number;
  size: "sm";
}

const { numPosts, size } = Astro.props;
---

<SeriesPartBadge numPosts={numPosts} size={size} />
